# Direct dependencies, pinned. Update deliberately — see README
# "Dependencies veilig updaten". Transitive deps resolve from these.
httpx==0.28.1
typer==0.26.8
fastapi==0.139.0
uvicorn[standard]==0.49.0
jinja2==3.1.6
PyYAML==6.0.3
python-dotenv==1.2.2
python-multipart==0.0.32
# Windows has no system timezone database; Compass needs Europe/Amsterdam.
tzdata==2025.2 ; platform_system == "Windows"
